# Tracing Feature

The canvas/interaction layer: captures finger and Apple Pencil touch points, renders stencils, and
wires the trace into the scoring engine. Reward feedback (stars/sound/animation) on completion.

Populated in the "tracing UI" build step.
