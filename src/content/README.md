# Content Data

Versioned JSON data files: character/number stencil paths and metadata for English, Hindi, and
numbers. Fully separate from UI and engine code — adding a new language or character set should
mean adding a data file here, never touching the tracing engine or UI components.

Populated in the "content data" build step.
